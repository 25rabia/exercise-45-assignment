var __makeTemplateObject = (this && this.__makeTemplateObject) || function (cooked, raw) {
    if (Object.defineProperty) { Object.defineProperty(cooked, "raw", { value: raw }); } else { cooked.raw = raw; }
    return cooked;
};
Hey;
Math.pow(, Day);
6;
Challenge: Start;
Math.pow(Coding, );
Math.pow(, Question);
16;
Math.pow(, More);
Guests: You;
've found a bigger dinner table, so there';
s;
room;
for (more; Math.pow(guests.
, Explain) & TIP; )
    : Math.pow(, When);
you;
have;
more;
space, you;
can;
add;
more;
guests;
to;
your;
list.You;
can;
add;
guests;
at;
the;
beginning, middle, and;
end;
of;
an;
Math.pow(array.
, Answer);
Please;
try { }
finally { }
to;
do
    it;
while (yourself);
Math.pow(first, )
    || ""(__makeTemplateObject(["typescript\nlet guests: string[] = [\"Albert Einstein\", \"Marie Curie\", \"Leonardo da Vinci\"];\nconsole.log(\"Great news! I found a bigger dinner table!\");\n\n// Adding more guests\nguests.unshift(\"Isaac Newton\");\nguests.splice(guests.length / 2, 0, \"Charles Darwin\");\nguests.push(\"Ada Lovelace\");\n\nguests.forEach(guest => {\n    console.log("], ["typescript\nlet guests: string[] = [\"Albert Einstein\", \"Marie Curie\", \"Leonardo da Vinci\"];\nconsole.log(\"Great news! I found a bigger dinner table!\");\n\n// Adding more guests\nguests.unshift(\"Isaac Newton\");\nguests.splice(guests.length / 2, 0, \"Charles Darwin\");\nguests.push(\"Ada Lovelace\");\n\nguests.forEach(guest => {\n    console.log("]));
Dear;
$;
{
    guest;
}
would;
you;
like;
to;
join;
me;
for (dinner ? ");\n});\n"(__makeTemplateObject([""], [""])) ||
    Math.pow(, Question) : ; 17; )
    : Math.pow(, Shrinking);
Guest;
List: Unfortunately, your;
new table;
won;
t;
arrive in time, and;
you;
can;
only;
invite;
two;
Math.pow(guests.
, Explain) & TIP;
Math.pow(, Sometimes);
plans;
change, and;
you;
Math.pow('ll need to adjust your guest list. Removing guests from your list is straightforward but should be done thoughtfully., Answer);
Please;
try { }
finally { }
to;
do
    it;
while (yourself);
Math.pow(first, )
    || ""(__makeTemplateObject(["typescript\nconsole.log(\"Unfortunately, I can only invite two people for dinner.\");\n\nwhile (guests.length > 2) {\n    let removedGuest = guests.pop();\n    console.log("], ["typescript\nconsole.log(\"Unfortunately, I can only invite two people for dinner.\");\n\nwhile (guests.length > 2) {\n    let removedGuest = guests.pop();\n    console.log("]));
Sorry, $;
{
    removedGuest;
}
I;
can;
't invite you to dinner.`);;
guests.forEach(function (guest) {
    console.log("Dear ".concat(guest, ", you're still invited to dinner."));
});
guests.splice(0, guests.length);
console.log(guests);
